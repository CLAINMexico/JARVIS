/**
 * Opciones necesarias para crear el bootstrap inicial de una app J.A.R.V.I.S.
 */
export interface BootstrapOptions {
  /**
   * Ruta del archivo settings.json que se debe cargar.
   *
   * Ejemplo:
   * ./settings.json
   */
  settingsFile: string;
}
