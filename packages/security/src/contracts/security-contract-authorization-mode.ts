/**
 * Modo de evaluación usado por el servicio de autorización.
 *
 * all = todos los valores requeridos deben existir.
 * any = al menos uno de los valores requeridos debe existir.
 */
export type SecurityAuthorizationMode = 'all' | 'any';
