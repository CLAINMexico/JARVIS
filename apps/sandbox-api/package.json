{
  "name": "@jarvis/sandbox-api",
  "version": "1.0.0",
  "private": true,
  "description": "Sandbox API for Developers.",
  "author": "CLAIN México, S.A. de C.V.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@jarvis/core": "workspace:*",
    "@jarvis/config": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
